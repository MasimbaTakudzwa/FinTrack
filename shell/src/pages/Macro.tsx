import { PagePlaceholder } from "./PagePlaceholder";

export function Macro() {
  return (
    <PagePlaceholder
      title="Macro"
      milestone="3E"
      description="FRED macro indicators — CPI, unemployment, Fed funds, 10Y treasury, GDP."
    />
  );
}
