import { PagePlaceholder } from "./PagePlaceholder";

export function Settings() {
  return (
    <PagePlaceholder
      title="Settings"
      milestone="3F"
      description="Data-source toggles, refresh intervals, clear cache, DB file path."
    />
  );
}
